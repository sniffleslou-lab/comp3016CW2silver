#pragma once

#include <GLFW/glfw3.h>

//called to resize winow
void framebuffer_size_callback(GLFWwindow* window, int width, int height);
//user input proccesses on aw window
void ProcessUserInput(GLFWwindow* WindowIn);

